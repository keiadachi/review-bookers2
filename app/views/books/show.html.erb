<div class="container">
  <div class="row">
    <div class="col-md-2 pt-3">

    <%= render 'users/info', user: @user %>
    <%= render 'books/newbook', book: @book %>
    </div>

    <div class='col-md-9 offset-1 pt-3'>
      <h2>Book detail</h2>
        <table class ="table">
          <tr>
            <td> <%= link_to user_path(@books.user) do %>
              <%= attachment_image_tag(@books.user, :image, :fill, 100, 100, fallback: "noimage.jpg") %><br>
              <%= @books.user.name %></td>
              <% end %>
            <td><%= @books.title %></td>
            <td><%= @books.opinion %></td>
            <td><%= link_to 'Edit', edit_book_path(@books) %></td>
            <td><%= link_to 'Destroy', book_path(@books), method: :delete  %></td>
          </tr>
        </table>
    </div>







  </div>
</div>